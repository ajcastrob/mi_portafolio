---
import Brand from "@/components/header/Brand.astro"
import Navbar from "@/components/header/Navbar.astro"
import HeaderButton from "@/components/header/HeaderButton.astro"

const linkLogo = "/assets/icons/logo.svg#logo"
const hamburgerIcon = "/assets/icons/menu.svg#menu" 
const closeIcon = "/assets/icons/menu.svg#close"
---

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: var(--z-header);
    width: 100%;
    max-width: 100%;
    padding: var(--space-16) var(--space-64) 0;
    background-color: var(--color-surface);
  }
  .header__container {
    position: relative;
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    width: 100%;
    max-width: var(--max-width-content);
    min-height: 64px;
    margin-inline: auto;
    padding: 0 var(--space-8) 0 var(--space-16);
    background-color: var(--color-surface);
    border-bottom: var(--border-width) solid var(--color-border);
  }

  .header__end {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: var(--space-8);
    flex-shrink: 0;
    min-width: 0;
  }

  /* Hamburger toggle — hidden on desktop */
  .header__toggle {
    display: none;
  }
  
    @media (max-width: 768px) {
    .header {
      padding: var(--space-12) var(--space-16) 0;
    }
    .btn {
    display: none;
     }
    .header__container {
      grid-template-columns: 1fr auto;
      padding: 0 var(--space-8);
    }
    .header__toggle {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 44px;
      height: 44px;
      padding: 0;
      background: transparent;
      border: none;
      border-radius: var(--radius-md);
      cursor: pointer;
      color: var(--color-text-primary);
      flex-shrink: 0;
      transition: background-color var(--duration) var(--ease-out);
    }
    .header__toggle:hover {
      background: var(--color-surface-warm);
    }
    .header__toggle svg {
      width: 24px;
      height: 24px;
    }
    .header__toggle-icon--close {
      display: none;
    }
    .header__toggle.is-open .header__toggle-icon--open {
      display: none;
    }
    .header__toggle.is-open .header__toggle-icon--close {
      display: block;
    }
    .header {
      z-index: calc(var(--z-header) + 2);
    }
    .header__end {
      position: relative;
      z-index: calc(var(--z-header) + 2);
    }

    @media (prefers-reduced-motion: reduce) {
      .header__toggle {
        transition: none;
      }
    }
  }
</style>

<header class="header">
  <div class="header__container">
    <Brand linkLogo={linkLogo} />      
    <Navbar />
    <div class="header__end">
      <button
        class="header__toggle"
        type="button"
        aria-label="Abrir menú"
        aria-expanded="false"
        aria-controls="site-nav"
        data-menu-toggle 
      >
        <svg class="header__toggle-icon--open" aria-hidden="true" focusable="false">
          <use href={hamburgerIcon}></use>
        </svg>
        <svg class="header__toggle-icon--close" aria-hidden="true" focusable="false" viewBox="0 0 50 50" width="24" height="24">
          <use href={closeIcon}></use>
        </svg>
      </button>
      <div class="btn">
        <HeaderButton />
      </div>
    </div>
  </div>
</header>

<script>
  const toggle = document.querySelector('[data-menu-toggle]');
  const nav = document.querySelector('#site-nav');
  const main = document.getElementById('contenido');
  const footer = document.getElementById('sitio-footer');
  const BREAKPOINT = 768;

  function getFocusable() {
    const items = [];
    if (toggle instanceof HTMLElement) items.push(toggle);
    nav?.querySelectorAll('a, button').forEach((el) => {
      if (el instanceof HTMLElement) items.push(el);
    });
    return items;
  }

  function openMenu() {
    nav?.setAttribute('data-menu-open', '');
    toggle?.setAttribute('aria-expanded', 'true');
    toggle?.setAttribute('aria-label', 'Cerrar menú');
    toggle?.classList.add('is-open');
    main?.setAttribute('inert', '');
    footer?.setAttribute('inert', '');
    document.body.style.overflow = 'hidden';
    const firstLink = nav?.querySelector('a');
    firstLink?.focus();
  }

  function closeMenu() {
    nav?.removeAttribute('data-menu-open');
    toggle?.setAttribute('aria-expanded', 'false');
    toggle?.setAttribute('aria-label', 'Abrir menú');
    toggle?.classList.remove('is-open');
    main?.removeAttribute('inert');
    footer?.removeAttribute('inert');
    document.body.style.overflow = '';
  }

  function toggleMenu() {
    if (nav?.hasAttribute('data-menu-open')) {
      closeMenu();
    } else {
      openMenu();
    }
  }

  toggle?.addEventListener('click', toggleMenu);

  document.addEventListener('keydown', (e) => {
    const open = nav?.hasAttribute('data-menu-open');
    if (!open) return;

    if (e.key === 'Escape') {
      closeMenu();
      toggle?.focus();
      return;
    }

    if (e.key !== 'Tab') return;
    const items = getFocusable();
    if (items.length === 0) return;
    const first = items[0];
    const last = items[items.length - 1];
    const active = document.activeElement;
    if (e.shiftKey && active === first) {
      e.preventDefault();
      last.focus();
    } else if (!e.shiftKey && active === last) {
      e.preventDefault();
      first.focus();
    }
  });

  nav?.addEventListener('click', (e) => {
    const target = e.target;
    if (target instanceof HTMLElement && target.closest('a')) {
      closeMenu();
    }
  });

  window.addEventListener('resize', () => {
    if (window.innerWidth > BREAKPOINT && nav?.hasAttribute('data-menu-open')) {
      closeMenu();
    }
  });
</script>
